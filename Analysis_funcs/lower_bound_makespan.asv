function lower_bound = lower_bound_makespan(a)
    num_progs = length(a) - 1;
    num_machines = a(length(a));
    max_prog = max(a(1:num_progs));
    
    average_cost = sum(a(1:num_progs))/num_progs;
    
    lower_bound = max([max_prog, average_cost
    
end

